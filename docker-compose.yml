services:
  rvtools-analyzer:
    build: .
    image: abeggi/rvtools-vibe:latest
    container_name: rvtools-analyzer
    ports:
      - "8080:8080"
    volumes:
      - ./rvtools_data:/app/data
    restart: unless-stopped
    environment:
      - DATA_DIR=/app/data
